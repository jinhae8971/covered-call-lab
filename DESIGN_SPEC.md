# 경제적자유 랩 — 디자인 전면 개편 명세서

> 이 문서는 **디자이너에게 넘기는 인수인계서**입니다. 현재 시스템이 무엇을 하고, 각 화면이 어떤 판단을 돕기 위해 존재하며, 무엇을 바꿔도 되고 무엇을 바꾸면 시스템이 깨지는지를 적었습니다.
> **레이아웃과 정보 구조까지 재해석해도 좋습니다.** 단 8장 「불변 제약」에 적힌 것만은 유지해 주세요.

---

## 0. 한 줄 요약

한국 투자자가 **커버드콜 ETF를 총수익률 관점에서 고르고, 자기 포트폴리오의 분배금이 늘고 있는지 줄고 있는지 판정하는** 단일 페이지 대시보드입니다. 미국·한국 상장 96개 종목을 주 1회 자동 수집하고, 보유 내역은 브라우저에만 저장합니다.

- 공개 주소: `jinhae8971.github.io/covered-call-lab`
- 표시명: **경제적자유 랩** (레포/URL은 `covered-call-lab` 유지)
- 부제: 총수익률 우선 리서치

---

## 1. 이 화면이 존재하는 이유

커버드콜 ETF는 **분배율만 보면 반드시 틀리는** 상품군입니다. 연 12% 분배금을 주면서 주가가 연 15% 깎이면 투자자는 자기 원금을 나눠 받으면서 세금까지 내고 있는 것인데, 통장 입금 내역만 보면 아무 이상이 없어 보입니다.

그래서 이 대시보드는 처음부터 끝까지 **하나의 편향**을 갖고 설계되어 있습니다.

> **분배율이 아니라 총수익률. 절대수익이 아니라 원지수 대비 초과수익.**

디자인이 바뀌어도 이 편향은 화면에서 읽혀야 합니다. 분배율 숫자가 총수익률보다 크고 화려하게 보이는 디자인은 이 제품의 주장을 배신합니다.

### 사용자가 이 화면에 와서 답을 얻고 싶은 5가지

| # | 질문 | 지금 답하는 곳 |
|---|---|---|
| Q1 | 커버드콜 ETF는 세대마다 뭘 고치려 했고, 실제로 나아졌나? | 분석 탭 — 세대별 계보, 세대별 평균 지표 |
| Q2 | 지금 살 종목을 무엇을 기준으로 줄 세워야 하나? | 분석 탭 — 종목 비교 테이블, 결론 |
| Q3 | 이번 달에 얼마가, 언제 들어오나? | 포트폴리오 탭 — 이번 달 분배금 |
| Q4 | 그 금액은 **늘고 있나 줄고 있나**? 원금을 갉아먹고 있진 않나? | 포트폴리오 탭 — 분배금 추세 |
| Q5 | 내 자산 배분은 지금 어떤 상태이고, 어디를 옮겨야 하나? | 포트폴리오 탭 — 보유 현황, NAV 추이, 현재 비중 |

Q4가 이 제품의 **차별점**입니다. 대부분의 배당 대시보드는 Q3에서 멈춥니다.

### 사용자

40대 한국인 개인투자자 1인(본인). 금융 리터러시는 높은 편이고 숫자를 피하지 않습니다. **모바일보다 데스크톱 사용이 압도적**이며, 한 번에 오래 들여다보고 판단을 내리는 용도입니다. 대충 훑는 화면이 아니라 **읽는 화면**입니다.

---

## 2. 기술 제약 (디자인 결정에 직접 영향)

| 항목 | 내용 |
|---|---|
| 산출물 | **단일 HTML 파일 1개.** 외부 CSS/JS 파일 없음. `index.html` 하나만 서빙 |
| 빌드 | `template.html` + `data/metrics.json` → Python(`build.py`)이 데이터를 주입해 `index.html` 생성 |
| 차트 | **Chart.js 4.4.1** (cdnjs). 유일한 외부 의존성 |
| 폰트 | **웹폰트 없음.** 시스템 폰트 스택만 사용 중 (한글 렌더링 안정성 + 로딩 지연 회피). 웹폰트를 쓰고 싶다면 한글 서브셋 용량을 근거와 함께 제시해 주세요 |
| 상태 저장 | `localStorage` 3개 키: `ccl_portfolio_v1`(보유내역) · `ccl_theme_v1`(테마) · `ccl_tab_v1`(마지막 탭) |
| 서버 | 없음. GitHub Pages 정적 호스팅. 백엔드 API 호출 0건 |
| 갱신 | GitHub Actions가 매주 토요일 아침 자동 재빌드 |
| 검증 | jsdom 회귀 테스트 123개 (`verify.js`)가 DOM 구조·차트 생성·판정 문구를 검사. **element ID를 바꾸면 테스트가 깨집니다** → 9장 참고 |
| 현재 규모 | `template.html` 104KB / 생성된 `index.html` 576KB (데이터 포함) |

### Chart.js의 알려진 함정 (재설계 시 반드시 인지)

Chart.js는 **CSS 변수를 읽지 못합니다.** 그래서 현재 코드는 `getComputedStyle`로 변수를 뽑아 JS 값으로 넘기는 `cssv(name)` 헬퍼를 두고, **테마를 바꿀 때 8개 차트를 전부 destroy 후 재생성**합니다. 차트 색을 CSS만으로 제어하는 디자인은 불가능합니다.

---

## 3. 정보 구조 (현행)

```
좌측 사이드바 (고정)
  브랜드 — [FL] 경제적자유 랩 / 총수익률 우선 리서치
  탭 2개 + 각 탭의 앵커 서브내비
  테마 토글 버튼
  메타 — 갱신일 · 수록 종목 수 · 환율

메인
  ├─ PANE 1 : ETF 분석      (기본 활성)
  │    KPI 스트립 7칸
  │    [카드] 세대별 계보
  │    [카드] 종목 비교 테이블        ← 16열 정렬·필터 테이블
  │    [카드×2] 분배율vs총수익 · 참여율vs방어폭
  │    [카드×2] NAV 침식 · 세대별 평균 지표
  │    [카드] 총수익률 우선 관점의 결론
  │    [카드] 한국 투자자 세금 구조
  │
  └─ PANE 2 : 내 포트폴리오
       ┌ 현금흐름 블록 (보유 0건이면 통째로 숨김)
       │  [카드] 이번 달 분배금 & 지급 예정일
       │  [카드] 분배금 추세 — 늘고 있나, 줄고 있나
       └
       [카드] 보유 현황                ← 입력 UI + KPI 4칸 + 12열 테이블
       ┌ 차트 블록 (보유 0건이면 숨김)
       │  [카드] 보유 종목 수익 추이
       │  [카드] 보유 종목 NAV 추이
       │  [카드] 현재 비중
       └

푸터 — 데이터 출처 · 투자 권유 아님 고지
```

**카드 14장 · 차트 8개 · 테이블 4개 · 세그먼트 토글 5개.**

### 카드 순서를 정한 원칙 — 숫자 하나에 주인 하나

포트폴리오 탭은 최근 재설계를 거쳤습니다. 이전에는 상단에 인컴 KPI 스트립이 있었는데, **그 첫 칸이 바로 아래 카드의 헤드라인 숫자와 D-day를 그대로 반복**했습니다. 같은 값이 두 곳에 있으면 정보가 두 배가 되는 게 아니라 사용자가 어느 쪽을 믿어야 할지 모르게 됩니다. 스트립을 통째로 걷어내고 소유권을 갈랐습니다.

| 카드 | 이 카드만 소유하는 숫자 |
|---|---|
| 이번 달 분배금 | 당월 수령액 · 지급 완료/잔여 건수 · 다음 지급 D-day |
| 분배금 추세 | 연 예상 수령액 · 월 평균 · 주당 분배금 증감 판정 |
| 보유 현황 | 총 평가금액 · 평가손익 · 평가익 과세 추정 · 리밸런싱 필요액 |

**재설계에서도 이 원칙은 지켜 주세요.** 같은 숫자를 두 카드가 보여주면 안 됩니다.

---

## 4. 화면별 상세 명세 — PANE 1 : ETF 분석

### 4-1. KPI 스트립 (7칸)

수록 종목 / 평균 분배율 TTM / 평균 총수익 1년 / 평균 초과수익 1년 / 평균 상승참여율 / 평균 상장후 주가 / 평균 MDD 1년.

각 칸은 `라벨 · 큰 값 · 작은 설명` 3단 구조입니다. **평균값은 전부 커버드콜 종목(G1~G4)만으로 계산**하고, 지수·채권·원자재·레버리지는 제외합니다 — 옵션을 매도하지 않는 자산을 커버드콜과 같은 자로 재면 통계가 오염되기 때문입니다.

> 재설계 여지: 7칸은 한 줄에 놓기에 많습니다. 중요도 위계를 넣거나(초과수익·총수익을 크게), 2단 그룹으로 나누는 걸 검토해 주세요.

### 4-2. 세대별 계보 카드

G1~G4 각각에 대해 `세대 라벨 · 문제의식(2~3문장) · 미니 지표 4개(종목수/분배율/총수익1Y/초과수익1Y)`를 카드로 나열합니다. 아래에 자산배분 축(BM·BD·CM·LV) 4개를 같은 형식으로 한 줄 더 놓고, "이들은 커버드콜과 같은 자로 재지 않는다"는 주석을 답니다.

> 이 카드는 **읽는 카드**입니다. 문제의식 텍스트가 핵심이고 숫자는 보조입니다. 지금은 숫자가 너무 작아 세대 간 개선 추이가 눈에 안 들어옵니다. **세대를 따라 초과수익이 실제로 올라갔는지**가 시각적으로 읽히면 가장 좋습니다.

### 4-3. 종목 비교 테이블 — 이 제품에서 가장 중요한 컴포넌트

**16열 × 최대 96행.** 정렬 가능(헤더 클릭), 필터 칩 3그룹(시장/세대/벤치마크 포함), 검색 인풋 1개.

열: 종목 / 분류 / 분배율TTM / 총수익1Y / **원지수대비 초과수익** / 상승참여율 / 낙폭방어폭 / 총수익3Y(연) / 총수익 상장후(연) / 주가변화 상장후 / MDD1Y / MDD전기간 / 변동성1Y / Calmar1Y / 보수 / **종합점수**

- 종합점수 = 초과수익 40% + 총수익1Y 15% + 낙폭방어폭 20% + 분배율 15% + 저보수 10%의 백분위 가중합. **커버드콜끼리만** 계산하며 BM·BD·CM·LV는 공란
- 종목 셀에는 자산군 색 점(●)과 시장 뱃지가 붙습니다
- 하단에 방법론 주석 5문단 (초과수익을 쓰는 이유, 참여율의 정의, NAV 침식 대용치, 데이터 출처)

> **가장 큰 디자인 과제입니다.** 16열은 노트북 화면에서 가로 스크롤이 불가피하고, 지금은 전 열이 같은 시각적 무게를 가져서 "무엇부터 봐야 하나"가 안 보입니다.
> 요청: **핵심 4열(초과수익·총수익1Y·낙폭방어폭·종합점수)을 앞세우고 나머지를 접거나 밀어내는 구조**, 혹은 행 단위 요약 카드 뷰 같은 대안을 제시해 주세요. 단 전체 열을 볼 수 있는 경로는 남아야 합니다.

### 4-4. 구조 차트 4종

| 차트 | 형태 | 읽는 법 |
|---|---|---|
| 분배율 vs 총수익률 | 산점도 | "고배당 = 고수익"이 거짓임을 보이는 차트. 상관이 없거나 음(-)이면 주장 성립 |
| 상승참여율 vs 낙폭방어폭 | 산점도 | 오른쪽 위가 잘 설계된 상품, 왼쪽 아래가 최악 조합. **사분면 해석이 핵심** |
| NAV 침식 | 가로 막대 | 왼쪽(음수)일수록 원금을 갉아먹는 중 |
| 세대별 평균 지표 | 그룹 막대 | 세대가 올라갈수록 초과수익이 개선됐나 |

> 산점도 2개는 **사분면 배경 구획과 축 기준선**이 있어야 의미가 읽힙니다. 지금은 점만 찍혀 있어 사용자가 직접 사분면을 상상해야 합니다.

### 4-5. 결론 카드 / 세금 구조 카드

결론은 데이터에서 자동 생성되는 인사이트 목록. 세금 구조는 3열 정적 비교표(구분 / 미국 직상장 / 국내 상장) 7행 + 실무 판단 기준 주석. **세금표는 이 대시보드에서 유일하게 데이터 갱신과 무관한 정적 콘텐츠**로, 문서처럼 읽히는 편이 자연스럽습니다.

---

## 5. 화면별 상세 명세 — PANE 2 : 내 포트폴리오

> 부제: "언제 얼마가 들어오는가를 먼저 봅니다 · 계좌 유형별 세후 · 보유 내역은 이 브라우저에만 저장됩니다"

### 5-1. 이번 달 분배금 & 지급 예정일

**좌우 2단.** 왼쪽은 요약 텍스트 블록, 오른쪽은 일정 리스트.

- 왼쪽: 당월 세후 예상 수령액(가장 큰 숫자) / 세전 / 계좌 유형 / 총 n건 중 m건 완료 / **다음 지급 D-day와 종목**
- 오른쪽: `지급 예상일 | 종목 · 분배기준일 | 예상 수령액(세후, 세전 병기)` 행 목록. 이미 지난 건은 흐리게(opacity .42)
- 상단 세그먼트: `이번 달 / 2개월 / 3개월` — **창을 넓혀도 왼쪽 요약은 항상 당월 기준 유지**
- 기준일은 이번 달이지만 입금이 다음 달로 넘어가는 건(국내 월말 기준일)은 별도 문장으로 안내
- 하단 주석: 지급일 추정 방법론

**상태:** 보유 0건 → 부모 블록째 숨김 / 당월 지급 예정 0건 → 빈 상태 메시지

### 5-2. 분배금 추세 — 늘고 있나, 줄고 있나 ★ 이 제품의 핵심 카드

세그먼트 토글 2개로 서로 다른 두 화면을 전환합니다.

#### 뷰 ① 주당 분배금 추이 (기본)

**요약 블록 → 라인 차트 → 판정표** 3단.

*요약 블록 문장 예시:*
> 보유 포트폴리오의 주당 분배금은 1년 전 대비 **+3.0%** — 거의 그대로입니다. (평가금액 가중 · TTM 12개월 이동합 기준)
> 연 예상 수령액 3,242,263원 · 월평균 270,189원 (일반계좌 세후)
> 감액 1종: QLD

증감 판정은 5단계 문장입니다: `줄고 있습니다 (≤−8%) / 조금씩 줄고 있습니다 (≤−3%) / 거의 그대로입니다 (<+3%) / 조금씩 늘고 있습니다 (<+8%) / 늘고 있습니다`.

*차트:* 종목별 TTM 주당분배금을 **24개월 전 = 100**으로 지수화한 라인. 금액 단위가 원/달러로 섞여 있어 절대값을 겹쳐 그릴 수 없기 때문입니다. **선이 우하향이면 주당 분배금이 실제로 줄고 있다**는 뜻이며, 보유 수량을 늘려 총액이 커진 것과는 다른 이야기입니다.

*판정표 6열:* `종목 | 최근 회차 · TTM 주당분배금 | 3개월 전 대비 | 전년 동기 대비 | 주가 1년 | 판정`

| 판정 배지 | 조건 | 현재 색 |
|---|---|---|
| **증액** | TTM DPS 전년 대비 ≥ +8% | 초록 |
| **횡보** | −8% ~ +8% | 회색 |
| **감액** | ≤ −8% | 빨강 |
| **원금 잠식** | DPS는 유지(>−8%)인데 주가 1년 ≤ −8% | 주황 |
| 이력 부족 | 전년 동기 비교 구간 없음 | 흐린 회색 |

> **「원금 잠식」이 이 카드의 존재 이유입니다.** 분배금은 그대로인데 NAV가 깎여 나가는 상태 — 통장만 보면 아무 이상이 없어 보이는 가장 위험한 구간입니다. 이 배지는 화면에서 **가장 눈에 띄어야 하는 단일 요소**입니다. 현재는 5개 배지가 같은 크기·형태라 위계가 없습니다.

#### 뷰 ② 월별 예상 수령액

향후 12개월 월별 세전/세후 막대 차트 + 최다/최소 달과 편차 배수 인사이트. 판정표는 숨깁니다.

> 두 뷰가 하나의 카드를 공유하는 게 맞는지도 재검토 대상입니다. 뷰①은 "늘고 있나"(판정), 뷰②는 "언제 들어오나"(계획)로 성격이 다릅니다.

### 5-3. 보유 현황

한 카드 안에 **입력·설정·결과**가 모두 들어 있어 현재 가장 밀도가 높습니다.

- 상단 액션 바: 계좌 유형 칩 3개(일반/연금/ISA) + 종목 추가 버튼 5개(커버드콜·지수·채권·원자재·레버리지) + 내보내기 / 불러오기 / **전체 삭제(위험 버튼)**
- KPI 4칸: 총 평가금액 / 평가손익 / 평가익 과세 추정 / 리밸런싱 필요액 — **분배 관련 수치는 한 개도 없습니다**(다른 카드 소유)
- 12열 테이블: 종목 / 보유수량 / 평균단가 / 현재가 / 평가금액(원) / 평가손익 / 수익률 / 연 예상배당 / 현재비중 / 목표비중 / 조정금액 / 삭제. **수량·평단·목표비중 3개 열은 인라인 입력 필드**입니다
- 빈 상태: "아직 보유 종목이 없습니다. 위의 ＋ 버튼으로…" + 로컬 저장 안내
- 계좌 유형이 연금/ISA인데 미국 직상장 종목이 있으면 **편입 불가 경고**

> 액션 버튼 9개가 한 줄에 늘어서 있어 `전체 삭제`가 `내보내기` 바로 옆에 있습니다. **파괴적 액션의 분리**가 필요합니다. 또한 "설정하는 영역"과 "결과를 읽는 영역"이 시각적으로 구분되지 않습니다.

### 5-4. 보유 종목 수익 추이

3분할 세그먼트: `총수익률(현금) / DRIP(재투자) / 주가수익률`. 종목별 지수선 + **현재 비중으로 합성한 포트폴리오 곡선을 굵은 선**으로 오버레이. 하단 주석에 세 값을 나란히 출력하고, 그 격차가 각각 분배금 몫과 재투자 복리 몫입니다.

### 5-5. 보유 종목 NAV 추이

2분할 세그먼트: `각자 상장 시점 = 100 / 공통 시점 = 100`. **100 기준선**이 의미의 전부입니다 — 기준선 아래로 내려간 종목은 분배금을 원금에서 꺼내 쓰고 있다는 뜻입니다. 하단에 종목별 `원금 변화 / 분배 기여 %p` 분해.

### 5-6. 현재 비중

2분할 세그먼트: `종목별 / 자산군별`. 도넛 차트 **중앙에 총 평가금액**. 하단에 HHI 집중도 지수와 경고(레버리지 15% 초과 · 커버드콜 60% 초과).

---

## 6. 데이터 계약 — `data/metrics.json`

디자인이 표현할 수 있는 것의 상한선입니다. **여기 없는 값은 만들어낼 수 없습니다.**

```
{ generated_at, fx:{usdkrw}, axis:[주간 날짜 157개], generations:{...}, rows:[96], errors:[] }
```

### row 주요 필드

| 필드 | 뜻 | 비고 |
|---|---|---|
| `id` `ticker` `name` `issuer` | 식별 | 한국은 6자리 코드 |
| `market` | `US` \| `KR` | |
| `gen` | `G1`~`G4` \| `BM` \| `BD` \| `CM` \| `LV` | 자산군 색의 근거 |
| `ccy` | `USD` \| `KRW` | 표시 통화 |
| `price` `asof` | 현재가 · 기준일 | |
| `ttm_yield` `ttm_dps` `pay_count_ttm` | 분배율 · 주당분배금 · 연 지급 횟수 | |
| `tr_ytd` `tr_1y` `tr_3y` `tr_5y` `tr_si` | 총수익률 (3Y·5Y·SI는 연환산) | |
| `px_si` `px_1y` | 주가 변화 = **NAV 침식 대용치** | |
| `mdd_si` `mdd_1y` `vol_1y` `calmar_1y` | 위험 지표 | |
| `excess_1y` `excess_3y` `excess_si` | **원지수 대비 초과수익** | 커버드콜만 |
| `capture_1y` `mdd_edge_1y` | 상승참여율 · 낙폭방어폭 | 커버드콜만 |
| `score` | 종합점수 0~100 | 커버드콜만 |
| `bm` `bm_name` | 비교 대상 원지수 | |
| `er` | 총보수 | |
| `ax` `axp` | 주간 시계열 2벌 — 배당재투자 조정 / 미조정 | 최대 157포인트 |
| `spark` | 스파크라인용 축약 시계열 | |
| `divhist` | **회차별 [날짜, 금액] 이력** (최대 3년) | 추세 판정의 원천 |
| `sched` | 지급 스케줄 `{ccy, annual, months:{월:비중}, paylag, basis}` | 캘린더의 원천 |
| `status` | `ok` \| 오류 | |

### 데이터 출처와 신뢰도 (화면에 표기 의무 있음)

- **미국**: Yahoo Finance 배당재투자 조정종가 + 실제 분배기준일(ex-date) 이력
- **한국**: 네이버 금융 수정주가 및 분배 금액 + **Yahoo의 `.KS` ex-date 이력**(46종목 중 42종목 확보). 출처가 둘로 갈리므로 **금액은 네이버, 날짜는 Yahoo**로 역할을 나누고, Yahoo TTM 합계가 네이버 값과 25% 넘게 어긋나면 신뢰하지 않고 관행 규칙으로 되돌린 뒤 `≈` 표기
- 지급일은 **전부 추정치**이며, 실입금은 기준일 + 영업일 3일 가정. 공휴일 미반영

> 이 "추정치임"과 "≈"는 **디자인에서 지워지면 안 되는 정직성 장치**입니다. 다만 지금은 긴 주석 문단에 묻혀 있어 아무도 안 읽습니다. 더 나은 표현을 제안해 주세요.

---

## 7. 현행 디자인 시스템 (2026-08 재설계 반영)

### 7-1. 컬러 토큰 (다크 = 기본)

```css
:root, html[data-theme="dark"]{
  /* 배경 · 표면 */
  --bg:#080b10        --side1:#0d1219     --side2:#080b10
  --surface:#111721   --surface2:#171e2b  --surface3:#1f2837
  --line:#212a38      --line2:#303c4e     --grid:#1b2230

  /* 텍스트 3단 */
  --tx:#eaeff6        --tx2:#94a2b6       --tx3:#78869d

  /* 액션 · 의미 */
  --acc:#5b9cf6       --acc2:#7c6cf0      --onacc:#08111f
  --pos:#2fbe74       --neg:#ff6060       --warn:#e5a92b

  /* 워시 (사분면 배경 · 경보 띠 · 선택 상태) */
  --wash:rgba(91,156,246,.10)
  --negwash:rgba(255,96,96,.10)
  --alertbg:rgba(229,169,43,.14)
  --alertln:rgba(229,169,43,.45)

  /* 차트 전용 */
  --pfline:#ffffff    /* 포트폴리오 합성 곡선 — 개별 종목선 위에 얹히는 강조선 */

  /* 형태 */
  --r:14px            --r2:9px            --r3:6px
  --shadow:0 1px 0 rgba(255,255,255,.025), 0 14px 30px -24px #000
}
```

라이트 테마는 **같은 이름의 변수만 갈아끼웁니다.** 컴포넌트 CSS는 전부 `var()`만 참조하므로 규칙을 두 벌 쓰지 않습니다. 반경 토큰(`--r/--r2/--r3`)만 테마 공통입니다.

```css
html[data-theme="light"]{
  --bg:#f2f5f9        --side1:#ffffff     --side2:#f2f5f9
  --surface:#ffffff   --surface2:#f5f8fc  --surface3:#e8eef6
  --line:#dde4ee      --line2:#c3cedd     --grid:#e9eef5
  --tx:#101825        --tx2:#4c5a6e       --tx3:#64728a
  --acc:#1d5fd6       --acc2:#5b3fe0      --onacc:#ffffff
  --pos:#0a8f4d       --neg:#cf2318       --warn:#8f5405
  --wash:rgba(29,95,214,.07)              --negwash:rgba(207,35,24,.07)
  --alertbg:rgba(143,84,5,.10)            --alertln:rgba(143,84,5,.34)
  --pfline:#101825
  --shadow:0 1px 2px rgba(16,24,40,.05), 0 10px 24px -18px rgba(16,24,40,.35)
}
```

**토큰 정합은 회귀 테스트가 강제합니다.** `verify.js` 의 "CSS 토큰 정합" 블록이 다크에만 있고 라이트에 없는 토큰을 실패로 잡습니다. 새 토큰을 추가할 때는 두 블록에 동시에 넣으세요.

### 7-2. 자산군 색 ★ 두 테마에서 **동일하게 고정**

```
--g1:#e0559b  1세대      --g2:#9a6ae8  2세대
--g3:#4b8ef0  3세대      --g4:#22c1c3  4세대
--bm:#8b95a5  지수        --bd:#6f9469  채권
--cm:#b8934a  원자재      --lv:#f2683c  레버리지
```

G1→G4 는 마젠타 → 퍼플 → 블루 → 틸 로 **한 방향 색상환 진행**입니다. 세대 순서가 색만으로 읽히도록 의도한 배치이므로 일부만 교체하면 계열 관계가 깨집니다. 자산배분 축 4색(BM·BD·CM·LV)은 채도를 낮춰 계보 4색과 섞이지 않게 분리했습니다.

JS 쪽 `GC` 상수가 같은 값을 중복 보유합니다 (Chart.js 가 CSS 변수를 읽지 못하기 때문). **한쪽만 바꾸면 표와 차트 색이 어긋납니다.**

### 7-3. 컴포넌트 인벤토리

| 클래스 | 역할 |
|---|---|
| `.app` `.side` `.main` | 셸 (980px 이하에서 사이드바 상단 접힘) |
| `.brand .chip/.t/.s` `.nav>button` `.bdg` `.subnav` `.sp` `.thbtn` `.side .meta` | 사이드바 · 탭 배지 · 테마 버튼 |
| `.pane` `.pane.on` `.phead` `.eyebrow` | 페인 전환 (`.pane` 기본 `display:none`) |
| `.card` `.card.flat` `.card.pad0` `.chead` `.csub` `.cnote` `.cdesc` | 카드 골격 |
| `.kpis` `.khero` `.khc` `.rk` `.rkb` `.rkb.out` `.vwrap` `.v` `.u` `.ex` | KPI 1·2순위 히어로 |
| `.kdiv` `.ksm` `.kpi` | KPI 3단 중 "참고 지표" 하단 |
| `.spine` `.base` `.zl` `.bars` `.bc` `.bw` `.bt` | 계보 스파인 (SVG polyline + 막대) |
| `.gen-row` `.gen` `.hd` `.dot` `.code` `.cnt` `.name` `.thesis` `.mg` | 세대 카드 |
| `.axrow` `.axpills` `.axpill` `.axnote` | 자산배분 축 (BM·BD·CM·LV) |
| `.ctl` `.cg` `.gl` `.chip` `.chip.on` `.shown` `.seg` `.btn` `.btn.acc` | 컨트롤 바 |
| `.tw` `tr.sum` `tr.open` `tr.det` `.det-in` `.dgrid` `.dgh` `.dgr2` `.dv-verdict` | 접히는 비교 테이블 (7열 + 상세행) |
| `.nmc` `.mkt` `.scv` `.bar-mini` `.exv` `.exb` `td.dim` `td.chev` | 테이블 셀 내부 |
| `.disc` `.disc-body` `.wrow` `.mgrid` `.mbox` | 방법론 disclosure (`<details>`) |
| `.alertbar` `.pill` | 포트폴리오 상단 경보 띠 (감액·원금잠식) |
| `.mncols` `.mnhd` `.mnrow` `.mnrow.est` `.mnbig` `.mnnext` `.mnempty` `.legend` `.infobox` | 이번 달 분배금 |
| `.dvsum` `.dvflags` `.vd` (`.up/.flat/.down/.ero/.na`) | 분배금 추세 요약 3칸 · 판정 배지 |
| `.pf-top` `.vr` `.pf-actions` `.dd` `.ddm` | 포트폴리오 툴바 · 드롭다운 2종 |
| `#pfkpi` `.wbox` `.slab` `#pftbl` `.pf-empty` `.xbtn` | 보유 내역 |
| `.ins` `.ins.good` `.ins.warn` `.ins .h/.b` | 자동 생성 인사이트 |
| `.stats` `.stat` `.stat.wide` `.nvhd` `.nvrow` `.hhi` `.awarn` `.taxg` | 부가 요약 블록 |
| `.pos` `.neg` `.warn` `.mut` `.mini` `.hint` `.note` `.sub` `.big` `.ar` | 의미 색 · 주석 텍스트 |

### 7-4. Chart.js 커스텀 플러그인 3종

| id | 대상 | 역할 |
|---|---|---|
| `quad<x>_<y>` | c1 (20, 0) · c2 (65, 0) | `beforeDatasetsDraw` — 사분면 워시 + 점선 십자선 + 네 모서리 라벨. "어느 칸에 있는 종목인가"가 곧 결론이므로 축 숫자보다 칸이 먼저 읽혀야 합니다. |
| `bl100` | cp4 | `afterDatasetsDraw` — y=100 점선 + "100 = 원금 보전" 라벨. 데이터셋이 아니라 플러그인으로 그려 범례 한 칸을 아끼고, 종목 색 순환을 흐트러뜨리지 않습니다. |
| `ctr` | cp3 (도넛) | `afterDraw` — 도넛 중앙에 총 평가금액. 범례만으로는 규모 감각이 안 잡힙니다. |

세 플러그인 모두 `cssv()` 로 색을 그때그때 읽으므로 테마 전환 시 차트 재생성만으로 따라옵니다.

## 8. 불변 제약 — 바꾸면 시스템이 깨집니다

1. **단일 HTML 파일.** 별도 CSS/JS 파일, 빌드 툴체인(Tailwind CLI·PostCSS 등) 도입 불가
2. **CSS 변수 기반 테마 전환.** 컴포넌트는 `var()`만 참조. 하드코딩 색상 금지
3. **자산군 8색은 두 테마 공통** (7-2)
4. **element ID 유지** — 회귀 테스트 123개가 참조합니다:
   `pane-research` `pane-portfolio` `kpis` `pfkpi` `s-gen` `s-tbl` `s-chart` `s-ins` `s-tax` `s-month` `s-div` `s-hold` `s-trend` `s-nav` `s-alloc` `pfcash` `pfcharts` `mnseg` `dvseg` `axseg` `navseg` `pieseg` `dvsum` `dvtbl` `dvwrap` `mnsum` `mnlist` `mnnote` `np1`~`np4` `thbtn` `tbl` `pftbl` `pfempty` `q` `gen` `cnt` `fxm`
   차트 캔버스: `c1` `c2` `c3` `c4` `cp1` `cp2` `cp3` `cp4`
   → **구조를 바꾸시려면 바꾼 ID 목록을 함께 주세요.** 테스트는 제가 맞추겠습니다
5. **숫자 하나에 주인 하나** (3장) — 같은 값을 두 카드가 표시하지 않음
6. **정직성 장치 유지** — 지급일 추정 표기, `≈` 마커, 데이터 출처 고지, 투자 권유 아님 고지
7. **보유 내역은 브라우저에만.** 서버 전송·외부 폰트/스크립트를 통한 데이터 유출 경로를 만들지 않음
8. **`tabular-nums`** — 모든 금액·비율 숫자는 고정폭 숫자로. 표에서 자릿수가 흔들리면 비교가 불가능합니다

---

## 9. 자유 영역 — 여기는 마음껏

- 레이아웃 골격 전체 (사이드바 유무, 탭 방식, 카드 그리드, 1단/2단)
- 카드 순서와 그룹핑 (단 3장 원칙 준수)
- 타이포그래피 스케일, 여백 리듬, 라운딩, 그림자, 보더 유무
- 차트 스타일 (Chart.js가 표현 가능한 범위 내)
- 테이블 표현 방식 — 접기/펼치기, 카드 뷰 전환, 열 우선순위, 고정 열
- 판정 배지·경고의 시각 위계
- 빈 상태 · 로딩 · 오류 상태의 표현
- 색 팔레트 전면 교체 (7-2 제약 하에 8색 세트로)
- 마이크로 인터랙션 (호버·전환·정렬 애니메이션)

---

## 10. 지금 불만인 것 — 재설계로 풀고 싶은 문제

우선순위 순입니다.

1. **비교 테이블 16열이 읽히지 않는다.** 무엇부터 봐야 하는지가 시각적으로 없음 (4-3)
2. **「원금 잠식」 배지가 다른 배지와 같은 무게다.** 이 제품이 유일하게 경고하고 싶은 상태인데 눈에 안 띔 (5-2)
3. **보유 현황 카드가 입력·설정·결과를 다 떠안고 있다.** 파괴적 액션이 일상 액션 옆에 있음 (5-3)
4. **주석(`.note`)이 너무 길고 아무도 안 읽는다.** 방법론과 정직성 고지가 전부 여기 묻혀 있음
5. **산점도 2개가 사분면 없이 점만 찍혀 있다** (4-4)
6. **KPI 스트립 7칸에 위계가 없다** (4-1)
7. **세대별 계보에서 "세대가 발전했다"가 안 읽힌다** (4-2)
8. 전반적으로 **모든 카드가 같은 시각적 무게**를 가짐 — 판단에 결정적인 카드와 참고용 카드가 구분되지 않음

---

## 11. 원하는 산출물

1. **화면 시안** — 최소한 ① 분석 탭 전체 ② 포트폴리오 탭 전체 ③ 비교 테이블 확대 ④ 분배금 추세 카드 확대. 다크/라이트 양쪽
2. **토큰 세트** — 7장 형식 그대로 CSS 변수 선언으로. 자산군 8색 포함
3. **컴포넌트 스펙** — 7-3 인벤토리 각각의 새 형태. 상태(기본/호버/활성/비활성)와 빈 상태 포함
4. **변경 목록** — 구조를 바꿨다면 어떤 ID가 어떻게 바뀌는지 (8장 4항)
5. 가능하면 **단일 HTML 프로토타입** — 그대로 `template.html`에 이식할 수 있으면 가장 좋습니다

### 수용 기준

- 다크·라이트 양쪽에서 WCAG AA 대비 충족 (본문 4.5:1, 큰 텍스트 3:1)
- 1440px 기준 설계, 1280px에서 깨지지 않을 것. 모바일은 열람 가능 수준이면 충분
- 자산군 8색이 두 테마에서 서로 구분될 것
- 96행 테이블에서 스크롤·정렬이 버벅이지 않을 것
- 화면을 처음 본 사람이 **"분배율이 아니라 총수익률을 보라"**는 이 제품의 주장을 레이아웃만으로 읽어낼 수 있을 것

---

*작성 기준: 2026-08-17 · 수록 96종목 · USD/KRW 1,415.7*
